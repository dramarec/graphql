import React from 'react'
// import { useQuery } from "@apollo/client";

export const Books = () => {

    // const { loading, data, error } = useQuery()

    return (
        <div>
            <h1>Books</h1>
        </div>
    )
}


